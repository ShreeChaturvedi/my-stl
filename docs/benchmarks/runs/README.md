# Benchmark Runs

This directory stores raw benchmark output and structured run files.

- `bench-*.json` -- parsed results + environment metadata
- `bench-*.log` -- raw stdout from `stl_bench`

Generated via `python3 scripts/bench/run.py`.
